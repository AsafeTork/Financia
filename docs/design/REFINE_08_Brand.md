# REFINE_08 — Brand & Identidade Visual

> ⚠️ Preencher seguindo `docs/design/TEMPLATE.md` (seções 0-8, ≥300 linhas).

## Objetivo
Consolidar/estender a identidade: logo system (uso, clearspace, cores/black/white), ícones
(pictogramas consistentes nas telas), ilustração leve de empty states/marketing (SVG inline),
dark mode da marca, favicons/icons PWA, assets public/ — sem gerar binários (ÁUDIOS, sem
imagens rasterizadas novas por enquanto; se precisar, especificar SVG).

## Contexto mínimo
- `VISUAL_IDENTITY.md` (fontes, cores, símbolo), `public/` (listar assets existentes)
- `src/index.css` (usos da marca: --brand-soft etc.), grep de ícones SVG inline espalhados
  (`src/features/**` amostra)
- grep "logo" em src/ (componentes de logo: Login, Landing, Header)

## Pesquisa obrigatória (≥10 buscas)
fintech logo system guidelines, icon consistency web app (stroke weight, size grid 16/20/24),
SVG illustration libraries lightweight (undraw morphological — mas avaliar peso e licença),
dark mode logo variants, favicon PWA sizes best practice 2026, "Visual Identity" gap analysis,
brand color systems with accessible derivatives.

## Retorno
`buscas=.., urls=.., lidos=.., doc_linhas=.. | top3 P0`